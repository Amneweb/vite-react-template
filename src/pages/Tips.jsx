import { Box, Typography } from "@mui/material";

const Tips = () => {
  return (
    <Box>
      <Typography>
        Caracteristicas de la bateria (amperaje, CCA, medidas, etc)
      </Typography>
      <Typography>Como puentear la bateria</Typography>
      <Typography>Precauciones</Typography>
    </Box>
  );
};

export default Tips;
